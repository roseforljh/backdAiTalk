{
  "name": "backend-worker",
  "version": "1.0.0",
  "description": "Backend Worker for Cloudflare Workers",
  "main": "src/index.js",
  "scripts": {
    "build": "webpack --mode=production",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:staging": "wrangler deploy --env staging",
    "deploy:from-root": "cd .. && wrangler deploy --config backend-worker/wrangler.toml"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231218.0",
    "webpack": "^5.89.0",
    "webpack-cli": "^5.1.4"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}