DEEPSEEK_KATEX_FORMATTING_INSTRUCTION = (
    "!! CRITICAL: MANDATORY KaTeX & Markdown Formatting Rules for Deepseek !!"
    "\n\n"
    "Your entire output MUST strictly follow these rules. This is a technical requirement, not a suggestion. Failure to comply will break the user's display."
    "\n\n"
    "--- I. MARKDOWN RULES ---"
    "\n\n"
    "1. **Headings:** Use `# Heading 1`, `## Heading 2`, etc. ALWAYS put a space after the `#`."
    "\n   - **FORBIDDEN:** `\\## Title`, `# # Title`."
    "\n\n"
    "2. **Lists:** Use a hyphen `- ` (hyphen followed by a space) for all bullet points."
    "\n\n"
    "--- II. KaTeX RULES FOR ALL MATH ---"
    "\n\n"
    "**A. INLINE MATH (math inside a sentence):**"
    "\n"
    "   - **MANDATORY FORMAT: `\\( ... \\)`**"
    "\n"
    "   - This is the ONLY accepted format for inline math. No exceptions."
    "\n"
    "   - **FORBIDDEN:** Do NOT use `[...]`, `$ ... $`, or bare parentheses `(...)` for math."
    "\n\n"
    "**B. DISPLAY MATH (math on its own line):**"
    "\n"
    "   - **MANDATORY FORMAT: ` ```math ... ``` `** (a fenced code block with the 'math' language identifier)."
    "\n"
    "   - This is the ONLY accepted format for display math."
    "\n"
    "   - **FORBIDDEN:** Do NOT use `$$ ... $$`, `\\[ ... \\]`, or just write the word `math` above a formula."
    "\n\n"
    "--- III. EXAMPLE OF WHAT NOT TO DO (AND HOW TO FIX IT) ---"
    "\n\n"
    "**WRONG OUTPUT (contains multiple errors):**"
    "\n"
    "The formula is [E=mc^2]. Also, the Pythagorean theorem is a^2 + b^2 = c^2."
    "\n"
    "And here is a naked formula:"
    "\n"
    "S_{\\text{大}} = 4 \\times \\left\\frac{1}{2} ab\\right + c^2 = 2ab + c^2"
    "\n"
    "$$"
    "\n"
    "\\sum_{i=1}^n i = \\frac{n(n+1)}{2}"
    "\n"
    "$$"
    "\n\n"
    "**CORRECT OUTPUT (how it MUST be formatted):**"
    "\n"
    "The formula is \\(E=mc^2\\). Also, the Pythagorean theorem is \\(a^2 + b^2 = c^2\\)."
    "\n"
    "And here is a naked formula:"
    "\n"
    "\\(S_{\\text{大}} = 4 \\times \\left\\frac{1}{2} ab\\right + c^2 = 2ab + c^2\\)"
    "\n"
    "```math"
    "\n"
    "\\sum_{i=1}^n i = \\frac{n(n+1)}{2}"
    "\n"
    "```"
    "\n\n"
    "--- FINAL CHECKLIST (MANDATORY) ---"
    "\n"
    "1. Is EVERY piece of math, no matter how small, wrapped in either `\\( ... \\)` or ` ```math ... ``` `?"
    "\n"
    "2. Have I avoided ALL forbidden formats (`[...]`, `$..$`, `$$..$$`, `\\[..\\]`, naked formulas)?"
    "\n"
    "Your precision is critical. Adhere strictly to these rules."
)