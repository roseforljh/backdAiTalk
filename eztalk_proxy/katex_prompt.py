KATEX_FORMATTING_INSTRUCTION = (
    "### KaTeX & Markdown Strict Formatting (for AI)\n\n"
    "**Follow these rules exactly for reliable mathematical and Markdown rendering.**\n\n"

    "# I. MARKDOWN SYNTAX\n"
    "- **Headings:** `# H1`, `## H2`, ..., up to `###### H6`. Must use 1 space after `#` at line start (0–3 leading spaces only).\n"
    "- **Lists:** Use `- item`, `* item` (unordered) or `1. item` (ordered). Each item starts on its own line.\n"
    "- **Bold/Italic:** Use `**bold**`, `__bold__`, `*italic*`, or `_italic_`.\n"
    "- **Links/Images:** `[text](url)`, `![alt](url)`.\n\n"

    "# II. INLINE MATH (`\\( ... \\)`)\n"
    "- **Wrap every math symbol, variable, or LaTeX command** in `\\( ... \\)`.\n"
    "    - Examples: `\\(x\\)`, `\\(15 \\div 3 = 5\\)`, `\\(\\vec{A} \\cdot \\vec{B}\\)`.\n"
    "- **What to exclude:**\n"
    "    - Pure numbers in common sentences (`5 apples`, `2025`): **no math wrapping**.\n"
    "    - Percentages in normal prose: `55%` (math mode only for formulas).\n"
    "- **Delimiters:**\n"
    "    - Use ONLY `\\( ... \\)` for inline math (not `$...$`, not `( ... )`).\n\n"

    "# III. DISPLAY MATH (`math` code block)\n"
    "- **Standalone (block) equations:** Use fenced code block with `math` tag:\n"
    "    ```math\n"
    "    ...latex here...\n"
    "    ```\n"
    "- **For multi-line/align:**\n"
    "    ```math\n"
    "    \\begin{aligned}\n"
    "      ...lines...\n"
    "    \\end{aligned}\n"
    "    ```\n"
    "- **Forbidden:** Do NOT use `$$ ... $$` or `\\[ ... \\]` for display math.\n\n"

    "# IV. MATH CHECKLIST\n"
    "- **All math symbols/commands inside `\\( ... \\)` (except in display blocks).**\n"
    "- **No unmatched `$`, `\\[`, `\\]`, or `\\( ... \\)`.**\n"
    "- **Never wrap whole sentences in math mode—only math content.**\n"
    "- **No plain brackets for math—always use `\\( ... \\)`.**\n\n"

    "# V. EXAMPLES\n"
    "1. **Correct (Inline):** The projection is `\\(\\vec{A}\\)`.\n"
    "2. **Display:**\n"
    "    ```math\n"
    "    \\int_0^\\infty e^{-x^2}\\,dx = \\frac{\\sqrt{\\pi}}{2}\n"
    "    ```\n"
    "3. **Natural Text:** I have 5 apples. (No math wrapping needed.)\n"
    "4. **Thermodynamics:** \\(\\Delta G = \\Delta H - T \\Delta S\\)\n\n"

    "# VI. FINAL REVIEW—BEFORE SENDING\n"
    "- [ ] All math expressions wrapped as specified?\n"
    "- [ ] Display formulas in ```math blocks only?\n"
    "- [ ] No forbidden delimiters (`$...$`, `\\[...\\]`, etc)?\n"
    "- [ ] Markdown syntax correct for headings/lists?\n"

    "Meticulously apply these rules. **Any deviation can break user experience.**\n"
)
