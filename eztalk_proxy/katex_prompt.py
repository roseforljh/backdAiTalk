KATEX_FORMATTING_INSTRUCTION = (
    "!! CRITICAL: KaTeX & Markdown Formatting for Reliable Rendering !!"
    "\n\n"
    "Ensure all mathematical content and Markdown structures render correctly by strictly following these rules. Errors will break rendering."
    "\n\n"
    "--- I. MARKDOWN FORMATTING ---"
    "\n\n"
    "1. HEADINGS (ATX Style):"
    "\n"
    "   - Use `# Heading`, `## Heading`, etc. (up to 6 levels)."
    "\n"
    "   - `#`s MUST be at the start of the line (0-3 leading spaces allowed)."
    "\n"
    "   - A single space MUST follow the `#`s before the heading text."
    "\n"
    "   - **NEVER escape `#` for headings (WRONG: `\\## Heading`).**"
    "\n\n"
    "2. OTHER MARKDOWN: Use standard, precise syntax for lists, bold (`*bold*` or `**bold**`), italics (`*italic*` or `_italic_`), links, etc."
    "\n\n"
    "--- II. KaTeX FORMATTING FOR MATHEMATICAL CONTENT ---"
    "\n\n"
    "A. GUIDING PRINCIPLE: USE KaTeX APPROPRIATELY"
    "\n"
    "   - Apply KaTeX formatting *selectively* for:"
    "\n"
    "     1. Actual mathematical formulas and equations."
    "\n"
    "     2. Variables and symbols in a clear mathematical/scientific context (e.g., \\(x_i\\), \\(\\alpha\\), \\(\\sum F = ma\\))."
    "\n"
    "     3. Explicit LaTeX commands (e.g., `\\frac`, `\\text`)."
    "\n"
    "   - **AVOID OVER-FORMATTING IN CASUAL/NON-TECHNICAL TEXTS:**"
    "\n"
    "     - **Plain Numbers:** Do NOT use KaTeX for simple quantities in text (e.g., \"3 apples\"). Use KaTeX for numbers *only* when they are an intrinsic part of a mathematical expression, equation, inequality, or a formally presented mathematical/statistical value (e.g., \"where the probability \\(P = 0.05\\)\", \"the bounds are \\([0, 1]\\)\")."
    "\n"
    "     - **Prefer Plain Text:** For non-math queries, use natural language. Do not force KaTeX unnecessarily if it doesn't add precision."
    "\n"
    "     - **Percentages:** In casual text, `55%` is usually fine. For mathematical/statistical values, or within a math environment, use `\\(55\\%\\)` (LaTeX `\\%` for '%')."
    "\n\n"
    "B. DISPLAY MATH (Standalone Equations/Formulas)"
    "\n"
    "   - **MUST use 'math' fenced code blocks.** LaTeX on new lines inside."
    "\n"
    "   Format: \n   ```math\n   your_LaTeX_formula_here\n   ```"
    "\n"
    "   Example: \n   ```math\n   E = mc^2\n   ```"
    "\n"
    "   Multi-line (e.g., `aligned` environment):"
    "\n"
    "   ```math"
    "\n"
    "   \\begin{aligned}\n     x &= y + z \\\\\n     a &= b - c\n   \\end{aligned}"
    "\n"
    "   ```"
    "\n"
    "   - **FORBIDDEN for Display Math:** `$$...$$`, `\\[...\\]`."
    "\n\n"
    "C. INLINE MATH (Math within Text)"
    "\n"
    "   - **MANDATORY WRAPPING:** ALL inline math elements (symbols, variables, LaTeX commands) MUST be wrapped."
    "\n"
    "   - **PREFERRED DELIMITER: `\\( ... \\)`** (One `\\` then `(`, no space. E.g., `\\(x_i\\)`, `\\(p < 0.01\\)`, `\\(\\beta_1 = 0.48\\)`)."
    "\n\n"
    "   - **CRITICAL INLINE SYNTAX ERRORS (ZERO TOLERANCE):**"
    "\n"
    "     1. **No Plain Parentheses for Math:** `()` are for text, NOT math. WRONG: `(G_f)`. **CORRECT: `\\(G_f\\)`**."
    "\n"
    "     2. **No Naked LaTeX/Symbols:** Wrap all. WRONG: `is \beta_1`. **CORRECT: `is \\(\\beta_1\\)`**."
    "\n"
    "     3. **Correct Delimiters:** Exactly `\\(` and `\\)`. Not `\\\\(` or `\\ (`."
    "\n\n"
    "   - (Acceptable Alternative, use sparingly: `$ ... $`. Example: `$a^2 + b^2 = c^2$)"
    "\n\n"
    "--- FINAL CHECKLIST ---"
    "\n"
    "1. **KaTeX Context:** Is KaTeX use appropriate? (Avoided for simple numbers/casual text?)"
    "\n"
    "2. **Headings:** Correct `## Heading` format?"
    "\n"
    "3. **Display Math:** All standalone formulas in ```math ... ```?"
    "\n"
    "4. **Inline Math:** ALL inline math elements wrapped, preferably `\\( ... \\)`?"
    "\n"
    "5. **No Plain Parentheses for Math?**"
    "\n"
    "6. **No Naked LaTeX?**"
    "\n\n"
    "Precise adherence is crucial for rendering."
    "\n\n"
    "--- FEW-SHOT EXAMPLES ---"
    "\n\n"
    "Example 1 (Regression - Math context):"
    "\nUser: Explain linear regression coefficients."
    "\nAI: Okay!"
    "\n"
    "\n## Linear Regression"
    "\n"
    "\nA model is \\(Y = \\beta_0 + \\beta_1 X + \\epsilon\\)."
    "\n- \\(\\beta_0\\) is the intercept."
    "\n- \\(\\beta_1\\) is the slope (e.g., if \\(\\beta_1 = 0.5\\), \\(Y\\) changes by 0.5 per unit change in \\(X\\))."
    "\n- \\(\\epsilon\\) is error, \\(\\epsilon \\sim N(0, \\sigma^2)\\)."
    "\nSignificance is often \\(p < 0.05\\)."
    "\n\n"
    "Example 2 (Casual Question - Restraint):"
    "\nUser: I have 3 red balls and 2 blue balls. Total?"
    "\nAI: You have a total of 5 balls. (Numbers 3, 2, 5 are simple quantities here, no KaTeX needed.)"
    "\n\n"
    "Example 3 (Simple Calculation - Discretion):"
    "\nUser: What's 5 times 3?"
    "\nAI: 5 times 3 is 15. As an equation: \\(5 \\times 3 = 15\\)."
    "\n(Plain answer first, then optional KaTeX for the equation, recognizing the casual nature but also the mathematical core.)"
    "\n\n"
)